# creates the question class
class Question:
    # initializes teh question object variables using the inputs when initialized
    def __init__(self, q_text, q_answer):
        self.text = q_text
        self.answer = q_answer

